/* DEAD FILE — safe to delete. Aug 27 2026.
 *
 * A scratch script used once, while writing tests/outreach-stats/test.mjs, to
 * read the fixture's real numbers off lib/outreach.js so the expected values in
 * that suite could be written from the code rather than guessed at.
 *
 * It is empty now because the Mac folder this repo lives on refuses `rm` from the
 * bridge session that made it (see the memory note "Mounted folders can't
 * overwrite or delete"). A copy also sits in _to_delete/floor-scratch/.
 *
 * Nothing imports it. Delete it in Cursor or Finder.
 */
export {};
